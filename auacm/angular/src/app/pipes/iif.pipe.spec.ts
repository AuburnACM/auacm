/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { IifPipe } from './iif.pipe';

describe('IifPipe', () => {
  it('create an instance', () => {
    const pipe = new IifPipe();
    expect(pipe).toBeTruthy();
  });
});
